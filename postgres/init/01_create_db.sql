-- This file runs automatically on first start.
-- The database named by POSTGRES_DB is already created by the entrypoint;
-- add any extra setup (extensions, schemas, seed data) here.

CREATE EXTENSION IF NOT EXISTS "uuid-ossp";
